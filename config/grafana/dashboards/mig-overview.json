{
  "annotations": {
    "list": []
  },
  "editable": true,
  "graphTooltip": 0,
  "panels": [
    {
      "type": "timeseries",
      "title": "GPU / MIG Compute Activity (%)",
      "gridPos": {
        "h": 8,
        "w": 12,
        "x": 0,
        "y": 0
      },
      "targets": [
        {
          "expr": "DCGM_FI_PROF_GR_ENGINE_ACTIVE",
          "legendFormat": "{{Hostname}} {{UUID}} {{GPU_I_PROFILE}}"
        }
      ],
      "fieldConfig": {
        "defaults": {
          "noValue": "0"
        }
      }
    },
    {
      "type": "timeseries",
      "title": "Frame Buffer Used (MiB)",
      "gridPos": {
        "h": 8,
        "w": 12,
        "x": 12,
        "y": 0
      },
      "targets": [
        {
          "expr": "DCGM_FI_DEV_FB_USED",
          "legendFormat": "{{Hostname}} {{UUID}} {{GPU_I_PROFILE}}"
        }
      ],
      "fieldConfig": {
        "defaults": {
          "noValue": "0"
        }
      }
    },
    {
      "type": "timeseries",
      "title": "GPU Utilization (%)",
      "gridPos": {
        "h": 8,
        "w": 12,
        "x": 0,
        "y": 8
      },
      "targets": [
        {
          "expr": "DCGM_FI_DEV_GPU_UTIL",
          "legendFormat": "{{Hostname}} {{UUID}} {{GPU_I_PROFILE}}"
        }
      ],
      "fieldConfig": {
        "defaults": {
          "noValue": "0"
        }
      }
    },
    {
      "type": "timeseries",
      "title": "FB Free (MiB)",
      "gridPos": {
        "h": 8,
        "w": 12,
        "x": 12,
        "y": 8
      },
      "targets": [
        {
          "expr": "DCGM_FI_DEV_FB_FREE",
          "legendFormat": "{{Hostname}} {{UUID}} {{GPU_I_PROFILE}}"
        }
      ],
      "fieldConfig": {
        "defaults": {
          "noValue": "0"
        }
      }
    }
  ],
  "schemaVersion": 39,
  "style": "dark",
  "tags": [
    "mig",
    "gpu",
    "a100"
  ],
  "templating": {
    "list": []
  },
  "time": {
    "from": "now-15m",
    "to": "now"
  },
  "title": "MIG Overview",
  "version": 1
}